apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.postgres.serviceName | default "adminer-postgres" }}
spec:
  selector:
    {{- include "postgres.labels" . | nindent 4 }}
  clusterIP: None
  ports:
    - name: db-port
      port: {{ .Values.global.postgres.port | default 5432 }}
