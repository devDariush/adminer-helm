apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.global.postgres.appName | default "adminer-postgres" }}
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
spec:
  {{- if .Values.replication.enabled }}
  replicas: {{ .Values.replication.replicas }}
  {{- else }}
  replicas: 1
  {{- end }}
  selector:
    matchLabels:
      {{- include "postgres.labels" . | nindent 6 }}
  serviceName: {{ .Values.global.postgres.serviceName | default "postgres" }}
  template:
    metadata:
      labels:
        {{- include "postgres.labels" . | nindent 8 }}
    spec:
      volumes:
      - name: scripts
        configMap:
          name: postgres-init
          defaultMode: 0755
      initContainers:
        - name: clone-replica
          image: postgres:18
          command: ["bash", "/scripts/clone-if-replica.sh"]
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data
            - name: REPLICATION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.postgres.appName | default "adminer-postgres" }}
                  key: replication-password
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql
            - name: scripts
              mountPath: /scripts
      containers:
        - name: postgres
          image: postgres:18
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql
            - name: scripts
              mountPath: /docker-entrypoint-initdb.d
          resources:
            requests:
              cpu: {{ .Values.resources.minimumCPU | default "100m" }}
              memory: {{ .Values.resources.minimumRAM | default "100Mi" }}
            limits:
              cpu: {{ .Values.resources.maximumCPU | default "1000m" }}
              memory: {{ .Values.resources.maximumRAM | default "1000Mi" }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.global.postgres.port | default 5432 }}
            initialDelaySeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready"]
            periodSeconds: 5
          ports:
            - name: http
              containerPort: {{ .Values.global.postgres.port | default 5432 }}
              protocol: TCP
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: {{ .Values.global.postgres.appName | default "adminer-postgres" }}
            - name: POSTGRES_PORT
              value: {{ .Values.global.postgres.port | default 5432 | quote }}
            - name: REPLICATION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.postgres.appName | default "adminer-postgres" }}
                  key: replication-password
            - name: PGDATA
              value: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes:
          {{- toYaml .Values.storage.accessModes | nindent 10 }}
        resources:
          requests:
            storage: {{ .Values.storage.capacity | default "1G" }}
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: {{ .Values.storage.whenDeleted }}
    whenScaled: {{ .Values.storage.whenScaled }}
