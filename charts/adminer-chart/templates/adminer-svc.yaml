apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.adminer.serviceName }}
spec:
  selector:
    {{- include "adminer.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.global.adminer.port }}
  {{/* Set additional specs: */}}
  {{- with .Values.service.extraSpec }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
